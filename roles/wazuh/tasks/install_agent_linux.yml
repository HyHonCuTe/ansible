---
# File: roles/wazuh/tasks/install_agent_linux.yml
# Mô tả: Cài đặt Wazuh Agent trên Linux (Ubuntu, Debian, CentOS, RHEL, Rocky, AlmaLinux)

# ==================== PREREQUISITES ====================
- name: "Cài đặt các gói phụ thuộc (Debian/Ubuntu)"
  ansible.builtin.apt:
    name:
      - gnupg
      - apt-transport-https
      - curl
      - lsb-release
    state: present
    update_cache: yes
  become: yes
  when: ansible_os_family == "Debian"

- name: "Cài đặt các gói phụ thuộc (RedHat/CentOS)"
  ansible.builtin.yum:
    name:
      - curl
      - gnupg
      - yum-utils
    state: present
  become: yes
  when: ansible_os_family == "RedHat"

# ==================== INSTALL WAZUH AGENT (Direct from URL) ====================
# Note: Installing specific version 4.7.5 to match manager version
- name: "Install Wazuh Agent from URL (Debian/Ubuntu)"
  ansible.builtin.apt:
    deb: "https://packages.wazuh.com/4.x/apt/pool/main/w/wazuh-agent/wazuh-agent_4.7.5-1_amd64.deb"
    state: present
  become: yes
  when: ansible_os_family == "Debian"
  environment:
    WAZUH_MANAGER: "{{ wazuh_manager_ip }}"
    WAZUH_AGENT_GROUP: "{{ wazuh_agent_group }}"
    WAZUH_AGENT_NAME: "{{ wazuh_agent_name }}"

- name: "Install Wazuh Agent from URL (RedHat/CentOS)"
  ansible.builtin.yum:
    name: "https://packages.wazuh.com/4.x/yum/wazuh-agent-4.7.5-1.x86_64.rpm"
    state: present
    disable_gpg_check: yes
  become: yes
  when: ansible_os_family == "RedHat"
  environment:
    WAZUH_MANAGER: "{{ wazuh_manager_ip }}"
    WAZUH_AGENT_GROUP: "{{ wazuh_agent_group }}"
    WAZUH_AGENT_NAME: "{{ wazuh_agent_name }}"

# ==================== CONFIGURE WAZUH AGENT ====================
- name: "Deploy Wazuh Agent configuration"
  ansible.builtin.template:
    src: wazuh_agent_linux_ossec.conf.j2
    dest: "{{ wazuh_agent_config_path_linux }}"
    owner: root
    group: wazuh
    mode: '0640'
  become: yes
  notify: Restart wazuh-agent

# ==================== CONFIGURE SELINUX (RHEL/CENTOS) ====================
- name: "Kiểm tra SELinux status"
  ansible.builtin.command: getenforce
  register: selinux_status
  changed_when: false
  failed_when: false
  when: ansible_os_family == "RedHat"

- name: "Configure SELinux cho Wazuh Agent"
  ansible.posix.seboolean:
    name: "{{ item }}"
    state: yes
    persistent: yes
  loop:
    - use_nfs_home_dirs
    - virt_use_nfs
  become: yes
  when:
    - ansible_os_family == "RedHat"
    - selinux_status.stdout == "Enforcing"
  failed_when: false

# ==================== REGISTER AGENT WITH MANAGER ====================
- name: "Register agent using agent-auth"
  ansible.builtin.shell: |
    /var/ossec/bin/agent-auth -m {{ wazuh_manager_ip }} -A {{ inventory_hostname }} -G {{ wazuh_agent_group }}
  become: yes
  register: agent_auth_result
  failed_when: false
  changed_when: "'Valid key received' in agent_auth_result.stdout or 'Valid key created' in agent_auth_result.stdout"

- name: "Debug agent-auth output"
  ansible.builtin.debug:
    var: agent_auth_result
  when: agent_auth_result.rc != 0

# ==================== START WAZUH AGENT ====================
- name: "Enable và start Wazuh Agent"
  ansible.builtin.systemd:
    name: "{{ wazuh_agent_service_linux }}"
    enabled: yes
    state: started
    daemon_reload: yes
  become: yes

# ==================== VERIFY INSTALLATION ====================
- name: "Chờ Wazuh Agent kết nối với Manager"
  ansible.builtin.wait_for:
    timeout: 30
  delegate_to: localhost
  connection: local
  become: false

- name: "Kiểm tra trạng thái Wazuh Agent"
  ansible.builtin.systemd:
    name: "{{ wazuh_agent_service_linux }}"
  register: wazuh_agent_status
  become: yes

- name: "Lấy thông tin Agent ID"
  ansible.builtin.command: /var/ossec/bin/agent-control -l
  register: agent_info
  changed_when: false
  failed_when: false
  become: yes

- name: "Hiển thị thông tin Wazuh Agent"
  ansible.builtin.debug:
    msg: |
      
      ╔════════════════════════════════════════════════════════════════╗
      ║           ✅ WAZUH AGENT INSTALLATION COMPLETED                ║
      ╚════════════════════════════════════════════════════════════════╝
      
      🖥️  Host:           {{ inventory_hostname }}
      🐧 OS:             {{ ansible_distribution }} {{ ansible_distribution_version }}
      📦 Agent Name:     {{ wazuh_agent_name }}
      👥 Agent Group:    {{ wazuh_agent_group }}
      
      🔗 Connection:
      ├─ Manager IP:     {{ wazuh_manager_ip }}
      ├─ Manager Port:   {{ wazuh_manager_port }}
      └─ Protocol:       {{ wazuh_manager_protocol | upper }}
      
      ✅ Service Status:  {{ 'Running' if wazuh_agent_status.status.ActiveState == 'active' else 'Stopped' }}
      
      📋 Verify Commands:
      ├─ Status:         sudo systemctl status {{ wazuh_agent_service_linux }}
      ├─ Logs:           sudo tail -f /var/ossec/logs/ossec.log
      └─ Agent Info:     sudo /var/ossec/bin/agent-control -l
      
      ═══════════════════════════════════════════════════════════════════
